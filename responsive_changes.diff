--- index.original.html

+++ index_responsive_fixed.html

@@ -48,7 +48,12 @@

        ============================================================ */
     *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
 
-    html { scroll-behavior: smooth; font-size: 16px; }
+    html {
+      scroll-behavior: smooth;
+      scroll-padding-top: 76px;
+      font-size: 16px;
+      overflow-x: hidden;
+    }
 
     body {
       font-family: var(--font-body);
@@ -56,13 +61,34 @@

       background: var(--warm-white);
       line-height: 1.65;
       -webkit-font-smoothing: antialiased;
-    }
-
-    img { display: block; width: 100%; height: auto; object-fit: cover; }
+      overflow-x: hidden;
+      min-width: 0;
+    }
+
+    img { display: block; max-width: 100%; width: 100%; height: auto; object-fit: cover; }
 
     a { color: inherit; text-decoration: none; }
 
     ul { list-style: none; }
+
+
+    /* Prevent small pieces of content from forcing the whole page wider than the viewport. */
+    main, section, header, footer,
+    .container, .hero-inner,
+    .hero-content, .hero-image-wrap,
+    .service-card, .why-card, .testimonial-card,
+    .warranty-badge-large, .financing-visual,
+    .service-form, .footer-brand {
+      min-width: 0;
+      max-width: 100%;
+    }
+
+    .hero-headline, .section-title, .section-subtitle,
+    .service-title, .why-card-title, .testimonial-quote,
+    .footer-tagline, .footer-contact-item,
+    .financing-visual-title, .financing-visual-sub {
+      overflow-wrap: break-word;
+    }
 
     /* ============================================================
        UTILITY CLASSES
@@ -71,7 +97,8 @@

       width: 100%;
       max-width: 1120px;
       margin: 0 auto;
-      padding: 0 20px;
+      padding-inline: clamp(16px, 5vw, 28px);
+      min-width: 0;
     }
 
     .section-label {
@@ -103,6 +130,7 @@

     .btn {
       display: inline-flex;
       align-items: center;
+      justify-content: center;
       gap: 8px;
       font-family: var(--font-display);
       font-size: 0.92rem;
@@ -114,9 +142,12 @@

       transition: background var(--transition), color var(--transition),
                   border-color var(--transition), transform var(--transition),
                   box-shadow var(--transition);
-      white-space: nowrap;
+      white-space: normal;
+      text-align: center;
       text-decoration: none;
-      line-height: 1;
+      line-height: 1.2;
+      max-width: 100%;
+      min-width: 0;
     }
 
     .btn:active { transform: scale(0.97); }
@@ -157,7 +188,19 @@

       padding: 16px 32px;
     }
 
-    section { padding: 72px 0; }
+    section { padding: clamp(56px, 9vw, 72px) 0; }
+
+    @media (min-width: 520px) {
+      .btn { white-space: nowrap; }
+    }
+
+    @media (max-width: 420px) {
+      .btn,
+      .btn-lg {
+        width: 100%;
+        padding-inline: 18px;
+      }
+    }
 
     /* ============================================================
        HEADER / NAV
@@ -175,7 +218,9 @@

       display: flex;
       align-items: center;
       justify-content: space-between;
+      gap: 16px;
       height: 64px;
+      min-width: 0;
     }
 
     /* REPLACE: Swap this text/logo with your real business logo image */
@@ -185,9 +230,14 @@

       gap: 10px;
       font-family: var(--font-display);
       font-weight: 800;
-      font-size: 1.08rem;
+      font-size: clamp(0.92rem, 2.4vw, 1.08rem);
       color: #fff;
       letter-spacing: -0.01em;
+      min-width: 0;
+      max-width: calc(100vw - 96px);
+      white-space: nowrap;
+      overflow: hidden;
+      text-overflow: ellipsis;
     }
 
     .nav-logo-icon {
@@ -199,22 +249,26 @@

       align-items: center;
       justify-content: center;
       font-size: 1.1rem;
+      flex: 0 0 auto;
     }
 
     .nav-links {
       display: none;
       align-items: center;
-      gap: 4px;
+      gap: 2px;
+      min-width: 0;
+      flex-shrink: 1;
     }
 
     .nav-links a {
       font-family: var(--font-display);
-      font-size: 0.88rem;
+      font-size: 0.84rem;
       font-weight: 600;
       color: rgba(255,255,255,0.78);
-      padding: 8px 13px;
+      padding: 8px 10px;
       border-radius: 6px;
       transition: color var(--transition), background var(--transition);
+      white-space: nowrap;
     }
 
     .nav-links a:hover {
@@ -224,6 +278,8 @@

 
     .nav-cta {
       display: none;
+      flex: 0 0 auto;
+      padding-inline: 18px;
     }
 
     .hamburger {
@@ -234,6 +290,7 @@

       display: flex;
       flex-direction: column;
       gap: 5px;
+      flex: 0 0 auto;
     }
 
     .hamburger span {
@@ -254,8 +311,10 @@

       display: none;
       flex-direction: column;
       background: var(--navy-mid);
-      padding: 12px 20px 24px;
+      padding: 12px clamp(16px, 5vw, 28px) 24px;
       border-top: 1px solid rgba(255,255,255,0.08);
+      width: 100%;
+      max-width: 100%;
     }
 
     .mobile-menu.open { display: flex; }
@@ -279,10 +338,22 @@

       justify-content: center;
     }
 
-    @media (min-width: 768px) {
+    @media (min-width: 1080px) {
       .hamburger { display: none; }
       .nav-links { display: flex; }
       .nav-cta { display: inline-flex; }
+    }
+
+    @media (min-width: 1080px) and (max-width: 1180px) {
+      .nav-logo { font-size: 0.96rem; }
+      .nav-links a {
+        font-size: 0.78rem;
+        padding-inline: 7px;
+      }
+      .nav-cta {
+        font-size: 0.82rem;
+        padding-inline: 14px;
+      }
     }
 
     /* ============================================================
@@ -293,7 +364,7 @@

       background: var(--navy);
       overflow: hidden;
       padding: 0;
-      min-height: 92vh;
+      min-height: min(92vh, 760px);
       display: flex;
       align-items: center;
     }
@@ -324,12 +395,12 @@

       position: relative;
       z-index: 2;
       width: 100%;
-      padding: 80px 20px 72px;
+      padding: clamp(64px, 12vw, 100px) clamp(16px, 5vw, 28px) clamp(56px, 10vw, 88px);
       max-width: 1120px;
       margin: 0 auto;
       display: grid;
-      grid-template-columns: 1fr;
-      gap: 48px;
+      grid-template-columns: minmax(0, 1fr);
+      gap: clamp(32px, 7vw, 48px);
       align-items: center;
     }
 
@@ -348,6 +419,9 @@

       padding: 6px 14px;
       border-radius: 50px;
       margin-bottom: 20px;
+      max-width: 100%;
+      white-space: normal;
+      overflow-wrap: break-word;
     }
 
     .hero-eyebrow::before {
@@ -389,6 +463,10 @@

       margin-bottom: 44px;
     }
 
+    @media (max-width: 420px) {
+      .hero-buttons { flex-direction: column; }
+    }
+
     /* Trust badges */
     .hero-badges {
       display: flex;
@@ -408,6 +486,18 @@

       font-size: 0.8rem;
       font-weight: 700;
       color: rgba(255,255,255,0.88);
+      max-width: 100%;
+      min-width: 0;
+      overflow-wrap: break-word;
+    }
+
+    @media (max-width: 420px) {
+      .hero-badges { flex-direction: column; }
+      .trust-badge {
+        width: 100%;
+        border-radius: var(--radius-sm);
+        align-items: flex-start;
+      }
     }
 
     .trust-badge-icon {
@@ -437,9 +527,9 @@

     /* Overlay text on hero card */
     .hero-card-badge {
       position: absolute;
-      bottom: 20px;
-      left: 20px;
-      right: 20px;
+      bottom: clamp(12px, 4vw, 20px);
+      left: clamp(12px, 4vw, 20px);
+      right: clamp(12px, 4vw, 20px);
       background: rgba(13,31,60,0.86);
       backdrop-filter: blur(8px);
       border-radius: var(--radius-sm);
@@ -447,7 +537,9 @@

       display: flex;
       align-items: center;
       justify-content: space-between;
+      flex-wrap: wrap;
       gap: 12px;
+      min-width: 0;
     }
 
     .hero-card-badge-text {
@@ -472,10 +564,15 @@

 
     @media (min-width: 900px) {
       .hero-inner {
-        grid-template-columns: 1fr 1fr;
-        min-height: 88vh;
-        padding: 100px 20px;
+        grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
+        min-height: min(88vh, 760px);
+        padding-block: 100px;
       }
+    }
+
+    @media (max-width: 420px) {
+      .hero-image-card { aspect-ratio: 4/3.35; }
+      .hero-card-badge-stat { font-size: 1.15rem; }
     }
 
     /* ============================================================
@@ -496,11 +593,11 @@

     }
 
     @media (min-width: 600px) {
-      .services-grid { grid-template-columns: 1fr 1fr; }
+      .services-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
     }
 
     @media (min-width: 1000px) {
-      .services-grid { grid-template-columns: repeat(4, 1fr); }
+      .services-grid { grid-template-columns: repeat(4, minmax(0, 1fr)); }
     }
 
     .service-card {
@@ -592,11 +689,11 @@

     }
 
     @media (min-width: 600px) {
-      .why-grid { grid-template-columns: 1fr 1fr; }
+      .why-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
     }
 
     @media (min-width: 960px) {
-      .why-grid { grid-template-columns: repeat(3, 1fr); }
+      .why-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); }
     }
 
     .why-card {
@@ -650,7 +747,7 @@

     }
 
     @media (min-width: 860px) {
-      .warranty-wrap { grid-template-columns: 1fr 1fr; }
+      .warranty-wrap { grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); }
     }
 
     .warranty-badge-large {
@@ -740,13 +837,13 @@

 
     .projects-grid {
       display: grid;
-      grid-template-columns: 1fr 1fr;
-      gap: 16px;
+      grid-template-columns: repeat(2, minmax(0, 1fr));
+      gap: clamp(12px, 3vw, 16px);
       margin-top: 48px;
     }
 
     @media (min-width: 768px) {
-      .projects-grid { grid-template-columns: repeat(3, 1fr); }
+      .projects-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); }
     }
 
     .project-card {
@@ -822,11 +919,11 @@

     }
 
     @media (min-width: 640px) {
-      .testimonials-grid { grid-template-columns: 1fr 1fr; }
+      .testimonials-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
     }
 
     @media (min-width: 960px) {
-      .testimonials-grid { grid-template-columns: repeat(3, 1fr); }
+      .testimonials-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); }
     }
 
     .testimonial-card {
@@ -903,7 +1000,7 @@

     }
 
     @media (min-width: 860px) {
-      .financing-inner { grid-template-columns: 1fr 1fr; }
+      .financing-inner { grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); }
     }
 
     .financing-highlights {
@@ -942,11 +1039,22 @@

     .financing-visual {
       background: var(--navy);
       border-radius: var(--radius-lg);
-      padding: 44px 36px;
+      padding: clamp(28px, 6vw, 44px) clamp(20px, 5vw, 36px);
       display: flex;
       flex-direction: column;
+      align-items: flex-start;
       gap: 22px;
       box-shadow: var(--shadow-md);
+    }
+
+    .financing-visual .btn {
+      align-self: flex-start;
+    }
+
+    @media (max-width: 520px) {
+      .financing-visual .btn {
+        width: 100%;
+      }
     }
 
     .financing-visual-title {
@@ -970,6 +1078,8 @@

       border: 1px solid rgba(76,175,80,0.35);
       border-radius: 8px;
       padding: 12px 16px;
+      max-width: 100%;
+      min-width: 0;
     }
 
     .financing-partner-icon {
@@ -999,7 +1109,7 @@

       background: #fff;
       border: 1px solid var(--gray-200);
       border-radius: var(--radius-lg);
-      padding: 40px 28px;
+      padding: clamp(28px, 6vw, 40px) clamp(18px, 5vw, 28px);
       box-shadow: var(--shadow-sm);
       max-width: 720px;
       margin: 0 auto;
@@ -1017,7 +1127,7 @@

     }
 
     @media (min-width: 580px) {
-      .form-row.two-col { grid-template-columns: 1fr 1fr; }
+      .form-row.two-col { grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); }
     }
 
     .form-group {
@@ -1043,6 +1153,7 @@

       border-radius: var(--radius-sm);
       padding: 12px 14px;
       width: 100%;
+      min-width: 0;
       transition: border-color var(--transition), background var(--transition);
       appearance: none;
     }
@@ -1181,6 +1292,10 @@

       margin-bottom: 32px;
     }
 
+    @media (max-width: 420px) {
+      .cta-banner .btn { width: 100%; }
+    }
+
     /* ============================================================
        FOOTER
        ============================================================ */
@@ -1198,11 +1313,11 @@

     }
 
     @media (min-width: 640px) {
-      .footer-inner { grid-template-columns: 1fr 1fr; }
+      .footer-inner { grid-template-columns: repeat(2, minmax(0, 1fr)); }
     }
 
     @media (min-width: 900px) {
-      .footer-inner { grid-template-columns: 2fr 1fr 1fr; }
+      .footer-inner { grid-template-columns: minmax(0, 2fr) minmax(0, 1fr) minmax(0, 1fr); }
     }
 
     .footer-brand {
@@ -1241,10 +1356,11 @@

 
     .footer-contact-item {
       display: flex;
-      align-items: center;
+      align-items: flex-start;
       gap: 8px;
       font-size: 0.88rem;
       color: rgba(255,255,255,0.7);
+      overflow-wrap: anywhere;
     }
 
     .footer-contact-item a:hover {
@@ -2008,19 +2124,30 @@

     const hamburger = document.getElementById('hamburger');
     const mobileMenu = document.getElementById('mobileMenu');
 
+    function closeMobileMenu() {
+      mobileMenu.classList.remove('open');
+      hamburger.classList.remove('open');
+      hamburger.setAttribute('aria-expanded', 'false');
+    }
+
     hamburger.addEventListener('click', function () {
       const isOpen = mobileMenu.classList.toggle('open');
       hamburger.classList.toggle('open', isOpen);
-      hamburger.setAttribute('aria-expanded', isOpen);
+      hamburger.setAttribute('aria-expanded', String(isOpen));
     });
 
     // Close mobile menu when a nav link is clicked
     document.querySelectorAll('.mobile-nav-link, .mobile-menu-cta').forEach(function (link) {
-      link.addEventListener('click', function () {
-        mobileMenu.classList.remove('open');
-        hamburger.classList.remove('open');
-        hamburger.setAttribute('aria-expanded', false);
-      });
+      link.addEventListener('click', closeMobileMenu);
+    });
+
+    // Keep the nav from staying open if the screen is resized into desktop layout.
+    window.addEventListener('resize', function () {
+      if (window.innerWidth >= 1080) closeMobileMenu();
+    });
+
+    document.addEventListener('keydown', function (e) {
+      if (e.key === 'Escape') closeMobileMenu();
     });
 
 
@@ -2046,7 +2173,11 @@

         }
       });
 
-      if (!allFilled) return;
+      if (!allFilled) {
+        const firstInvalid = serviceForm.querySelector('[required][style*="border-color"]');
+        if (firstInvalid) firstInvalid.focus();
+        return;
+      }
 
       // Hide form and show success
       serviceForm.style.display = 'none';